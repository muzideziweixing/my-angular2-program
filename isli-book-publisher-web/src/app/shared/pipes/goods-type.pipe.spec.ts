import { GoodsTypePipe } from './goods-type.pipe';

describe('GoodsTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GoodsTypePipe();
    expect(pipe).toBeTruthy();
  });
});
