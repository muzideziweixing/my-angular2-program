import { GetFocusDirective } from './get-focus.directive';

describe('GetFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new GetFocusDirective();
    expect(directive).toBeTruthy();
  });
});
