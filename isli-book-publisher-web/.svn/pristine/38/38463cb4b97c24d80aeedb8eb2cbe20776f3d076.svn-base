import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ImageTextListBoxComponent } from './image-text-list-box.component';

describe('ImageTextListBoxComponent', () => {
  let component: ImageTextListBoxComponent;
  let fixture: ComponentFixture<ImageTextListBoxComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ImageTextListBoxComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ImageTextListBoxComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
