import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DetailsTipsComponent } from './details-tips.component';

describe('DetailsTipsComponent', () => {
  let component: DetailsTipsComponent;
  let fixture: ComponentFixture<DetailsTipsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DetailsTipsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DetailsTipsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
