import { FloatOnlyDirective } from './float-only.directive';

describe('FloatOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
