import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AccountAllDataTableComponent } from './account-all-data-table.component';

describe('AccountAllDataTableComponent', () => {
  let component: AccountAllDataTableComponent;
  let fixture: ComponentFixture<AccountAllDataTableComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AccountAllDataTableComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AccountAllDataTableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
