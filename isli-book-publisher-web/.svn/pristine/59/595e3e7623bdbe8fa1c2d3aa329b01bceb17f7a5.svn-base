import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AccountAllAddComponent } from './account-all-add.component';

describe('AccountAllAddComponent', () => {
  let component: AccountAllAddComponent;
  let fixture: ComponentFixture<AccountAllAddComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AccountAllAddComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AccountAllAddComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
