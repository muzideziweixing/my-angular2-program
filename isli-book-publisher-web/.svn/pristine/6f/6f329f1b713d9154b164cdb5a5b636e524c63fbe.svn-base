import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { NowTemplateComponent } from './now-template.component';

describe('NowTemplateComponent', () => {
  let component: NowTemplateComponent;
  let fixture: ComponentFixture<NowTemplateComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ NowTemplateComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(NowTemplateComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
