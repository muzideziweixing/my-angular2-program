import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ImageUploadBoxComponent } from './image-upload-box.component';

describe('ImageUploadBoxComponent', () => {
  let component: ImageUploadBoxComponent;
  let fixture: ComponentFixture<ImageUploadBoxComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ImageUploadBoxComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ImageUploadBoxComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
