import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RightLiNavComponent } from './right-li-nav.component';

describe('RightLiNavComponent', () => {
  let component: RightLiNavComponent;
  let fixture: ComponentFixture<RightLiNavComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RightLiNavComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RightLiNavComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
