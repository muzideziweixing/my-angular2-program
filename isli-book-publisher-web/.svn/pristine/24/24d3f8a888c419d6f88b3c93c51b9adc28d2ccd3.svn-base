import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DownloadRecordComponent } from './download-record.component';

describe('DownloadRecordComponent', () => {
  let component: DownloadRecordComponent;
  let fixture: ComponentFixture<DownloadRecordComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DownloadRecordComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DownloadRecordComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
